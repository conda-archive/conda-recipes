package:
  name: azure-nspkg
  version: "1.0.0"

source:
  fn: azure-nspkg-1.0.0.zip
  url: https://pypi.python.org/packages/source/a/azure-nspkg/azure-nspkg-1.0.0.zip
  md5: cb046beb44c3efdb8bf1688af6c011ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azure-nspkg = azure-nspkg:main
    #
    # Would create an entry point called azure-nspkg that calls azure-nspkg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - azure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Azure/azure-sdk-for-python
  license: Apache Software License
  summary: 'Microsoft Azure Namespace Package [Internal]'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
