package:
  name: pytest-capturelog
  version: !!str 0.7

source:
  fn: pytest-capturelog-0.7.tar.gz
  url: https://pypi.python.org/packages/source/p/pytest-capturelog/pytest-capturelog-0.7.tar.gz
  md5: cfeac23d8ed254deaeb50a8c0aa141e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  #preserve_egg_dir: True
  #entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pytest-capturelog = pytest-capturelog:main
    #
    # Would create an entry point called pytest-capturelog that calls pytest-capturelog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py >=1.1.1

  run:
    - python
    - py >=1.1.1

test:
  # Python imports
  # imports:

  #commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/memedough/pytest-capturelog/overview
  license:  MIT License
  summary: 'py.test plugin to capture log messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
