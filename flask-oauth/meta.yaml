package:
  name: flask-oauth
  version: "0.12"

source:
  fn: Flask-OAuth-0.12.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-OAuth/Flask-OAuth-0.12.tar.gz
  md5: eea1295eb191f3d3064c61df5a10cdc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-oauth = flask-oauth:main
    #
    # Would create an entry point called flask-oauth that calls flask-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - oauth2

  run:
    - python
    - flask
    - oauth2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mitsuhiko/flask-oauth
  license: BSD License
  summary: 'Adds OAuth support to Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
