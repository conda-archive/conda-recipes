package:
  name: nibabel
  version: 1.3.0

source:
  fn: nibabel-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nibabel/nibabel-1.3.0.tar.gz
  md5: e2b84b8f74683f9c53ce9a316acafc9f

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - distribute

  run:
    - python
    - numpy
    - pydicom

test:
  # Python imports
  imports:
    - nibabel

about:
  home: http://nipy.org/nibabel
  license: MIT and BSD Software License

