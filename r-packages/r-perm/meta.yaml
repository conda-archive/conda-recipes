{% set posix = 'm2-msys2-' if win else '' %}
{% set native = 'm2-mingw-w64-' if win else '' %}

package:
  name: r-perm
  # Note that conda versions cannot contain -, so any -'s in the version have
  # been replaced with _'s.
  version: "1.0_0.0"

source:
  fn: perm_1.0-0.0.tar.gz
  url:
    - http://cran.r-project.org/src/contrib/perm_1.0-0.0.tar.gz
    - http://cran.r-project.org/src/contrib/Archive/perm/perm_1.0-0.0.tar.gz


  # You can add a hash for the file here, like md5 or sha1
  # md5: 49448ba4863157652311cc5ea4fea3ea
  # sha1: 3bcfbee008276084cbb37a2b453963c61176a322
  # patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: coin
requirements:
  build:
    - r

  run:
    - r

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('perm')" # [not win]
    - "\"%R%\" -e \"library('perm')\"" # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  #home:
  license: GPL
  summary: Perform Exact or Asymptotic permutation tests
  license_family: GPL3

# The original CRAN metadata for this package was:

# Package: perm
# Type: Package
# Title: Exact or Asymptotic permutation tests
# Version: 1.0-0.0
# Date: 2010-07-29
# Author: Michael Fay
# Maintainer: Michael Fay <mfay@niaid.nih.gov>
# Depends: R (>= 2.2.1), stats
# Suggests: coin
# Description: Perform Exact or Asymptotic permutation tests
# License: GPL
# Packaged: 2010-07-29 15:28:08 UTC; faym
# Repository: CRAN
# Date/Publication: 2010-07-29 15:46:08
# NeedsCompilation: no

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
