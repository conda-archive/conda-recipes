package:
  name: requests-futures
  version: "0.9.5"

source:
  fn: requests-futures-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/r/requests-futures/requests-futures-0.9.5.tar.gz
  md5: e20dc6f063c70888a7f8225e349b6682
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requests-futures = requests-futures:main
    #
    # Would create an entry point called requests-futures that calls requests-futures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.0
    - futures >=2.1.3

  run:
    - python
    - requests >=1.2.0
    - futures >=2.1.3

test:
  # Python imports
  imports:
    - requests_futures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ross/requests-futures
  license: Apache Software License
  summary: 'Asynchronous Python HTTP for Humans.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
