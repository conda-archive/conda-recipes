package:
  name: schema
  version: 0.2.0

source:
  git_url: https://github.com/halst/schema.git
  git_tag: 0.2.0

build:

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - schema

about:
  home: https://github.com/halst/schema
  license: MIT
