package:
  name: svg.path
  version: "2.0b1"

source:
  fn: svg.path-2.0b1.zip
  url: https://pypi.python.org/packages/source/s/svg.path/svg.path-2.0b1.zip
  md5: 900a981902fe0bb09534dcefdbba0ebc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - svg.path = svg.path:main
    #
    # Would create an entry point called svg.path that calls svg.path.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - svg
    - svg.path
    - svg.path.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/regebro/svg.path
  license: CC0 1.0 Universal
  summary: 'SVG path objects and parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
