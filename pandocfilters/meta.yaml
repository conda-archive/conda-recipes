package:
  name: pandocfilters
  version: "1.2.3"

source:
  fn: pandocfilters-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pandocfilters/pandocfilters-1.2.3.tar.gz
  md5: 85ad8f9432071312b214d8a75f20c450
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pandocfilters = pandocfilters:main
    #
    # Would create an entry point called pandocfilters that calls pandocfilters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pandocfilters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jgm/pandocfilters
  license: BSD License
  summary: 'Utilities for writing pandoc filters in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
