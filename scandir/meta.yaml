package:
  name: scandir
  version: "1.1"

source:
  fn: scandir-1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/scandir/scandir-1.1.tar.gz
  md5: 6b6b777bcc119429e8f86bfe54be2635
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - scandir = scandir:main
    #
    # Would create an entry point called scandir that calls scandir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benhoyt/scandir
  license: BSD License
  summary: 'scandir, a better directory iterator and faster os.walk()'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
