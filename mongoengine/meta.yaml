package:
  name: mongoengine
  version: "0.10.5"

source:
  fn: mongoengine-0.10.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoengine/mongoengine-0.10.5.tar.gz
  md5: 51759662a230d37edb5907f4f9082e41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoengine = mongoengine:main
    #
    # Would create an entry point called mongoengine that calls mongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.7.1

  run:
    - python
    - pymongo >=2.7.1

test:
  # Python imports
  imports:
    - mongoengine
    - mongoengine.base
    - mongoengine.queryset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - blinker
    - coverage ==3.7.1
    - nose
    - pillow >=2.0.0
    - python-dateutil
    - rednose
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mongoengine.org/
  license: MIT License
  summary: 'MongoEngine is a Python Object-Document Mapper for working with MongoDB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
