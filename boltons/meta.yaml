package:
  name: boltons
  version: "15.0.1"

source:
  fn: boltons-15.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boltons/boltons-15.0.1.tar.gz
  md5: 66038b7250a62db24a34a2e29c120b9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boltons = boltons:main
    #
    # Would create an entry point called boltons that calls boltons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boltons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mahmoud/boltons
  license: BSD
  summary: "When they're not builtins, they're boltons."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
