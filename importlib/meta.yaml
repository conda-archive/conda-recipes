package:
  name: importlib
  version: "1.0.3"

source:
  fn: importlib-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/importlib/importlib-1.0.3.tar.gz
  md5: 3ddefaed1eea78525b9bd4ccf194881d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importlib = importlib:main
    #
    # Would create an entry point called importlib that calls importlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Python Software Foundation License
  summary: 'Backport of importlib.import_module() from Python 2.7'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
