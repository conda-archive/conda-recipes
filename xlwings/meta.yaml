package:
  name: xlwings
  version: "0.4.0"

source:
  fn: xlwings-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/x/xlwings/xlwings-0.4.0.tar.gz
  md5: 38cf19791dd71aadf38d3490a274196a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xlwings = xlwings:main
    #
    # Would create an entry point called xlwings that calls xlwings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - comtypes
    - pywin32

  run:
    - python
    - comtypes
    - pywin32

test:
  # Python imports
  imports:
    - xlwings
    - xlwings.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://xlwings.org
  license: BSD License
  summary: 'Make Excel fly: Interact with Excel from Python and vice versa.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
