package:
  name: flask-oauthlib
  version: "0.9.1"

source:
  fn: Flask-OAuthlib-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-OAuthlib/Flask-OAuthlib-0.9.1.tar.gz
  md5: 8723b0d49ea9550ab77a3b0711bb7a43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-oauthlib = flask-oauthlib:main
    #
    # Would create an entry point called flask-oauthlib that calls flask-oauthlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - oauthlib >=0.6.2
    - requests-oauthlib >=0.4.1

  run:
    - python
    - flask
    - oauthlib >=0.6.2
    - requests-oauthlib >=0.4.1

test:
  # Python imports
  imports:
    - flask_oauthlib
    - flask_oauthlib.contrib
    - flask_oauthlib.contrib.client
    - flask_oauthlib.provider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - flask-sqlalchemy
    - mock
    - nose
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lepture/flask-oauthlib
  license: BSD License
  summary: 'OAuthlib for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
