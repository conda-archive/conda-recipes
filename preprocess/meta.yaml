package:
  name: preprocess
  version: "1.1.0"

source:
  git_url: https://github.com/hplgit/preprocess.git

build:
  number: 1

requirements:
  build:
    - python
    - future

  run:
    - python
    - future

test:
  imports:
    - preprocess

about:
  home: http://code.google.com/p/preprocess/
  license: MIT
  summary: 'preprocess: a multi-language preprocessor'
