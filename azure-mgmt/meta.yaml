package:
  name: azure-mgmt
  version: "0.20.1"

source:
  fn: azure-mgmt-0.20.1.zip
  url: https://pypi.python.org/packages/source/a/azure-mgmt/azure-mgmt-0.20.1.zip
  md5: 8a678938c5e3f809392fe23e3653e83f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azure-mgmt = azure-mgmt:main
    #
    # Would create an entry point called azure-mgmt that calls azure-mgmt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - azure-mgmt-compute ==0.20.0
    - azure-mgmt-network ==0.20.1
    - azure-mgmt-resource ==0.20.1
    - azure-mgmt-storage ==0.20.0

  run:
    - python
    - azure-mgmt-compute ==0.20.0
    - azure-mgmt-network ==0.20.1
    - azure-mgmt-resource ==0.20.1
    - azure-mgmt-storage ==0.20.0

test:
  # Python imports
  imports:
    - azure
    - azure.mgmt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Azure/azure-sdk-for-python
  license: Apache Software License
  summary: 'Microsoft Azure Resource Management Client Libraries for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
