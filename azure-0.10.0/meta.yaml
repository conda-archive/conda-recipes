package:
  name: azure
  version: "0.10.0"

source:
  fn: azure-0.10.0.zip
  url: https://pypi.python.org/packages/source/a/azure/azure-0.10.0.zip
  md5: 305b0036df5696d68369807835f554ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azure = azure:main
    #
    # Would create an entry point called azure that calls azure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python-dateutil
    - pyopenssl
    - futures

  run:
    - python
    - python-dateutil
    - pyopenssl
    - futures

test:
  # Python imports
  imports:
    - azure
    - azure.http
    - azure.servicebus
    - azure.servicemanagement
    - azure.storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WindowsAzure/azure-sdk-for-python
  license: Apache Software License
  summary: 'Microsoft Azure client APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
