package:
  name: pyrss2gen
  version: !!str 1.1

source:
  fn: PyRSS2Gen-1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PyRSS2Gen/PyRSS2Gen-1.1.tar.gz
  md5: 3529f831c6a4ed717b55315974e16317
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  #preserve_egg_dir: True
  #entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyrss2gen = pyrss2gen:main
    #
    # Would create an entry point called pyrss2gen that calls pyrss2gen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - PyRSS2Gen

  #commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dalkescientific.com/Python/PyRSS2Gen.html
  license: BSD License
  summary: Generate RSS2 using a Python data structure

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
