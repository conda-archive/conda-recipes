package:
  name: simplejson
  version: 3.3.0

source:
  fn: simplejson-3.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simplejson/simplejson-3.3.0.tar.gz
  md5: 0e29b393bceac8081fa4e93ff9f6a001
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplejson = simplejson:main
    #
    # Would create an entry point called simplejson that calls simplejson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplejson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simplejson/simplejson
  license: ademic Free License (AFL) or MIT License

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
