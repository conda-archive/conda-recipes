package:
  name: flask-httpauth
  version: "2.5.0"

source:
  fn: Flask-HTTPAuth-2.5.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-HTTPAuth/Flask-HTTPAuth-2.5.0.tar.gz
  md5: 1e073a607fa9f023d55a84fd6570ad3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-httpauth = flask-httpauth:main
    #
    # Would create an entry point called flask-httpauth that calls flask-httpauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/miguelgrinberg/flask-httpauth/
  license: BSD License
  summary: 'Basic and Digest HTTP authentication for Flask routes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
