package:
  name: virtualenvwrapper
  version: "4.5.1"

source:
  fn: virtualenvwrapper-4.5.1.tar.gz
  url: https://pypi.python.org/packages/source/v/virtualenvwrapper/virtualenvwrapper-4.5.1.tar.gz
  md5: 5ff62b23d051767d351484db2c1fcb75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - virtualenvwrapper = virtualenvwrapper:main
    #
    # Would create an entry point called virtualenvwrapper that calls virtualenvwrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - virtualenvwrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Enhancements to virtualenv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
