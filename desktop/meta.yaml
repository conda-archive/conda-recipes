package:
  name: desktop
  version: "0.4.2"

source:
  fn: desktop-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/desktop/desktop-0.4.2.tar.gz
  md5: 6db32ed3a430157cac5dfa09edb49440
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - desktop = desktop:main
    #
    # Would create an entry point called desktop that calls desktop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - desktop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/desktop
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Simple desktop integration for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
