package:
  name: python-logstash
  version: !!str 0.4.2

source:
  fn: python-logstash-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-logstash/python-logstash-0.4.2.tar.gz
  md5: 30350b384a006c1d1adea6f7df90bf47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-logstash = python-logstash:main
    #
    # Would create an entry point called python-logstash that calls python-logstash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - logstash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vklochan/python-logstash
  license: MIT License
  summary: 'Python logging handler for Logstash.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
