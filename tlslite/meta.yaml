package:
  name: tlslite
  version: "0.4.8"

source:
  fn: tlslite-0.4.8.tar.gz
  url: https://pypi.python.org/packages/source/t/tlslite/tlslite-0.4.8.tar.gz
  md5: 36c13858ea63f262c4e4291c2f9ae38f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tlslite = tlslite:main
    #
    # Would create an entry point called tlslite that calls tlslite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tlslite
    - tlslite.integration
    - tlslite.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trevp.net/tlslite/
  license: public domain and BSD
  summary: 'tlslite implements SSL and TLS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
