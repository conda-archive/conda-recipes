package:
  name: veusz
  version: 1.21.0

source:
  git_url: https://github.com/jeremysanders/veusz.git

requirements:
  build:
    - python
    - numpy
    - qt
    - pyqt

  run:
    - python
    - numpy
    - pyqt

build:
  entry_points:
    - veusz = veusz.veusz_main:run

app:
  type: desk
  own_environment: true