package:
  name: pytest-localserver
  version: "0.3.3"

source:
  fn: pytest-localserver-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pytest-localserver/pytest-localserver-0.3.3.tar.gz
  md5: 44507e8db3456bffbe9d645135facb58
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pytest-localserver = pytest-localserver:main
    #
    # Would create an entry point called pytest-localserver that calls pytest-localserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug
    - pyopenssl

  run:
    - python
    - setuptools
    - werkzeug
    - pyopenssl

test:
  # Python imports
  imports:
    - pytest_localserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/basti/pytest-localserver/
  license: MIT License
  summary: 'py.test plugin to test server connections locally.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
