package:
  name: zc.lockfile
  version: !!str 1.1.0

source:
  fn: zc.lockfile-1.1.0.zip
  url: https://pypi.python.org/packages/source/z/zc.lockfile/zc.lockfile-1.1.0.zip
  md5: 8e46b830417363501363bd09e1af1ecc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.lockfile = zc.lockfile:main
    #
    # Would create an entry point called zc.lockfile that calls zc.lockfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.lockfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/zc.lockfile
  license: Zope Public License
  summary: 'Basic inter-process locks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
