package:
  name: itsdangerous
  version: "0.24"

source:
  fn: itsdangerous-0.24.tar.gz
  url: https://pypi.python.org/packages/source/i/itsdangerous/itsdangerous-0.24.tar.gz
  md5: a3d55aa79369aef5345c036a8a26307f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - itsdangerous = itsdangerous:main
    #
    # Would create an entry point called itsdangerous that calls itsdangerous.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mitsuhiko/itsdangerous
  license: BSD License
  summary: 'Various helpers to pass trusted data to untrusted environments and back.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
