package:
  name: netifaces
  version: "0.10.4"

source:
  fn: netifaces-0.10.4.tar.gz
  url: https://pypi.python.org/packages/source/n/netifaces/netifaces-0.10.4.tar.gz
  md5: 36da76e2cfadd24cc7510c2c0012eb1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netifaces = netifaces:main
    #
    # Would create an entry point called netifaces that calls netifaces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/al45tair/netifaces
  license: MIT License
  summary: 'Portable network interface information.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
