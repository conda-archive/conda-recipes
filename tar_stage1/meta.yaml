package:
  name: tar_stage1
  version: 1.27

source:
  fn: tar-1.27.tar.xz
  url: ftp://ftp.gnu.org/gnu/tar/tar-1.27.tar.xz

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

  string:
     build_on_debian_wheezy__p33
     
requirements:
  build:
    - gmp_stage1
    
about:
  home: http://www.gnu.org/software/tar/tar.html
  license: GPL3
