package:
  name: pyzolib
  version: 0.2.4

source:
  fn: pyzolib-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyzolib/pyzolib-0.2.4.tar.gz
  md5: 9bdad5a9861e14acc2864ba499072f57
#  patches:
   # List any patch files here
   # - fix.patch



  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    # requires python3
    # If setuptools is required to run setup.py, add distribute to the build
    # requirements.

  run:
    - python

test:
  imports:
    - pyzolib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/iep/
  license: BSD License

# See
# https://github.com/ContinuumIO/conda/blob/master/docs/source/build.rst for
# more information about meta.yaml
